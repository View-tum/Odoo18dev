<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_validate_billing_multi" model="ir.actions.server">
        <field name="name">Validate</field>
        <field name="model_id" ref="account_billing.model_account_billing"/>
        <field name="binding_model_id" ref="account_billing.model_account_billing"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
        <field name="code">
            for rec in records:
                rec.validate_billing()
        </field>
    </record>
</odoo>