<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hr_expense_sheet_form_inherit_cash_domain" model="ir.ui.view">
        <field name="name">hr.expense.sheet.form.inherit.cash.domain</field>
        <field name="model">hr.expense.sheet</field>
        <field name="inherit_id" ref="hr_expense.view_hr_expense_sheet_form" />
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <!-- visible if posted (approved) and amount_residual > 0 -->
                <button name="action_pay_with_petty_cash"
                    string="Pay with Petty Cash"
                    type="object"
                    class="oe_highlight"
                    invisible="state != 'post' or payment_state in ['paid', 'reversed']" />
            </xpath>


            <xpath expr="//field[@name='employee_journal_id']" position="attributes">
                <attribute name="domain">[('type', 'in', ['purchase', 'cash'])]</attribute>
            </xpath>

            <!-- Make Tax Info Visible in Expense Lines -->
            <xpath expr="//field[@name='expense_line_ids']/list/field[@name='date']"
                position="after">
                <field name="tax_number" optional="show" />
                <field name="tax_date" optional="show" />
            </xpath>
        </field>
    </record>
</odoo>
