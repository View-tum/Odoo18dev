<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_discount_single_line" model="ir.ui.view">
        <field name="name">account.move.form.discount.single.line</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@id='aml_tab']/field[@name='line_ids']" position="attributes">
                <attribute name="domain">[('discount_allocation_hide', '=', False)]</attribute>
                <attribute name="context">{
                    'default_move_type': context.get('default_move_type'),
                    'line_ids': line_ids,
                    'journal_id': journal_id,
                    'default_partner_id': commercial_partner_id,
                    'default_currency_id': currency_id or company_currency_id,
                    'kanban_view_ref': 'account.account_move_line_view_kanban_mobile',
                    'hide_discount_allocation_lines': True
                }</attribute>
            </xpath>

        </field>
    </record>
</odoo>
