<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Overwrite the original view to ensure it's removed from the DB -->
    <record id="mrp_mold_operation_form" model="ir.ui.view">
        <field name="name">mrp.routing.workcenter.mold.form</field>
        <field name="model">mrp.routing.workcenter</field>
        <field name="inherit_id" ref="mrp.mrp_routing_workcenter_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='description']" position="after">
                <field name="is_parallel_active" invisible="1"/>
            </xpath>
        </field>
    </record>

    <!-- Also delete the one I created by mistake if it exists -->
    <delete model="ir.ui.view" id="mrp_mold_operation_form_clean"/>
</odoo>
