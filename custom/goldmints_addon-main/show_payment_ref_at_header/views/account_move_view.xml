<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Show Payment Reference under Journal in the account move header -->
        <record id="view_move_form_payment_reference_header" model="ir.ui.view">
            <field name="name">account.move.form.payment.reference.header</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='journal_div']" position="after">
                    <field name="payment_reference"
                           invisible="move_type not in ('out_invoice', 'out_refund')"
                           readonly="inalterable_hash"
                           placeholder="Standard communication"/>
                </xpath>

                <xpath expr="//page[@id='other_tab']//group[@name='sale_info_group']//field[@name='payment_reference']" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
