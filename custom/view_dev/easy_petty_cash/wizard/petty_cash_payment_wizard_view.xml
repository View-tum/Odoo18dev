<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_petty_cash_payment_wizard_form" model="ir.ui.view">
        <field name="name">petty.cash.payment.wizard.form</field>
        <field name="model">petty.cash.payment.wizard</field>
        <field name="arch" type="xml">
            <form string="Pay">
                <!-- Invisible fields -->
                <field name="petty_cash_id" invisible="1" />
                <field name="transaction_type" invisible="1" />
                <field name="currency_id" invisible="1" />
                <field name="is_cheque_method" invisible="1" />

                <!-- Two-column layout matching Standard Odoo Pay popup -->
                <group>
                    <group name="group1">
                        <field name="payment_journal_id" string="Journal"
                            options="{'no_create': True, 'no_open': True}" required="1" />
                        <field name="payment_method_line_id" string="Payment Method"
                            required="1"
                            options="{'no_create': True, 'no_open': True}" />
                    </group>
                    <group name="group2">
                        <label for="amount" />
                        <div class="o_row">
                            <field name="amount" widget="monetary"
                                options="{'currency_field': 'currency_id'}" readonly="1" />
                        </div>
                        <field name="payment_date" string="Payment Date" required="1" />
                    </group>
                </group>

                <!-- Cheque Details (shown when cheque method selected) -->
                <group string="Cheque Details" invisible="not is_cheque_method">
                    <group>
                        <field name="cheque_id"
                            required="is_cheque_method"
                            options="{'no_create': True}" />
                    </group>
                    <group>
                        <field name="cheque_date"
                            required="is_cheque_method" />
                    </group>
                </group>

                <footer>
                    <button name="action_confirm_payment" string="Create Payment" type="object"
                        class="oe_highlight" />
                    <button string="Discard" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_petty_cash_payment_wizard" model="ir.actions.act_window">
        <field name="name">Pay</field>
        <field name="res_model">petty.cash.payment.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
