
# delivery_pickup_transport

Two very simple Odoo 18 models + A4 PDF reports:

- **Send to Warehouse** (`delivery.send`)
- **Transporter Pickup** (`transport.pickup`)

No states / attrs. Each form can be printed from a button on the form view.
